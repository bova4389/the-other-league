# The Other League

Everything for this site lives under `Sleeper FF/The Other League/`. The repo root
`index.html` is only a redirect stub.

Workspace-wide rules (cache busting, git setup, hooks) are in `C:\Users\bovac\code\CLAUDE.md`,
which Claude loads automatically as a parent folder.

@Sleeper FF/The Other League/CLAUDE.md
